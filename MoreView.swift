import SwiftUI

struct MoreView: View {
    var body: some View {
        ScrollView {
            Text("More Options Coming Soon")
                .font(.title)
                .padding()
        }
    }
}